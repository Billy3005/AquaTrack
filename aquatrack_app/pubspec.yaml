name: aquatrack_app
description: The hydration app that feels alive

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^13.0.0
  dio: ^5.4.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  tflite_flutter: ^0.10.4
  camera: ^0.10.5
  # firebase_core: ^2.27.0
  # firebase_auth: ^4.17.0
  # firebase_messaging: ^14.7.0
  fl_chart: ^0.67.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true