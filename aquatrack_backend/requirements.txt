# FastAPI và dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]==2.5.0  # [email] pulls email-validator, needed by EmailStr fields
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt,argon2]==1.7.4  # security.py hashes with argon2 -> needs argon2-cffi
python-multipart==0.0.6

# CORS handled by Starlette's built-in CORSMiddleware (ships with FastAPI) —
# no extra package needed; python-cors/fastapi-cors were unused and unpublished.

# AI Integration
openai==1.6.1
anthropic==0.39.0  # >=0.8 needed for Messages API (client.messages.create)
# ollama (local dev AI) intentionally omitted: it pins httpx>=0.27 which clashes
# with httpx==0.25.2. ai_coach_service imports it under try/except (ollama=None)
# and guards every call with `if ollama:`, so production runs fine without it.

# Production Features
psutil==5.9.6  # System monitoring
jinja2==3.1.2  # Email templates

# Utils
python-dotenv==1.0.0
httpx==0.25.2

# Image processing
Pillow==10.1.0

# Google Sign-In ID token verification (ADR 0006).
# [requests] extra pulls the `requests` lib that google.auth.transport.requests
# needs at verify time — without it token verification raises and login 401s.
google-auth[requests]>=2.35.0

